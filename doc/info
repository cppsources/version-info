ФОРМАТ ИНФОРМАЦИИ О ВЕРСИИ
    - основная информация (VERSION):
        версия ПО в числовом представлении (major.minor.patch.build)

    - дополнительная информация (VERSION_ADDITIONAL):
        полное и краткое название ПО,
        версия компилятора,
        дата и время последней сборки,
        версия Qt,
        перечень и версии сторонних библиотек (qwt, poppler и т.д.),
        информация об авторских правах,
        произвольные текстовые данные

ХРАНЕНИЕ ОСНОВНОЙ И ДОПОЛНИТЕЛЬНОЙ ИНФОРМАЦИИ
    - ОС на базе ядра Linux и ОС семейства Windows:
        основная и дополнительная информация хранится в соответствующих переменных в исходном коде главного pro-файла проекта, и
        вкомпиливается в исполняемый файл или в статическую/динамическую библиотеку (если было обращение к соответствующим переменным)

    - ОС на базе ядра Linux:
        основная информация добавляется в имя файла динамической библиотеки (например, libname.so.a.b.c) и симлинки на него (например, libname.so.a.b, libname.so.a, libname.so)

    - ОС семейства Windows:
        основная информация хранится как ресурс в исполняемом файле или динамической библиотеке

ОБНОВЛЕНИЕ ОСНОВНОЙ И ДОПОЛНИТЕЛЬНОЙ ИИНФОРМАЦИИ
    - вручную:
        редактирование разработчиком, соответствующих переменных

    - автоматически:
        редактирование сервером сборки, соответствующих переменных

ИСПОЛЬЗОВАНИЕ ОСНОВНОЙ И ДОПОЛНИТЕЛЬНОЙ ИНФОРМАЦИИ
    см. примеры
    
ФОРМАТ ВЫВОДА ИИНФОРМАЦИИ О ВЕРСИИ (НОСИТ РЕКОМЕНДУЕМЫЙ ХАРАКТЕР)
    - графическое приложение (все ОС):
        информация о версии отображается в правом верхнем углу главного окна приложения в виде: сборка <N> от <DD.MM.YYYY>, где N - номер сборки, DD, MM, YYYY - день, месяц и год сборки соответсвенно. При щелчке ЛКМ всплывает окно с информацией о версии в следующем виде:
            <полное название ПО>, сборка <N> от <DD.MM.YYYY>,
            <информация об авторских правах>,
            компилятор: <версия компилятора>,
            дата и время последней сборки: <дата и время последней сборки>,
            <версия Qt>, <перечень и версии сторонних библиотек (qwt, poppler и т.д.)>,
            <произвольные текстовые данные>

    - графическое или консольное приложение, запущенное с ключом коммандной строки -v или --version (все ОС):
        при запуске приложения  выводится в таком виде:
            см. вывод информации из предыдущего пункта, при щелчке ЛКМ

    - статическая/динамическая библиотека (все ОС):
        предоставляет методы, возвращающие значения полей основной и дополнительной информации. Способ и формат вывода определяется разработчиком
